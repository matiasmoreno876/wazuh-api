{
  "name": "wazuh-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run buildrun",
    "build": "tsc",
    "buildrun": "npm run build && node ./dist/index.js",
    "develop": "nodemon -e ts --exec \",npm run buildrun\" ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^20.1.0"
  },
  "devDependencies": {
    "@types/hapi__hapi": "^20.0.5",
    "@types/node": "^14.14.22",
    "nodemon": "^2.0.7",
    "typescript": "^4.1.3"
  }
}
